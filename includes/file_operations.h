
int insert_record (char* , char*, char*);

int delete_record (char*);

int update_record (char*, char*, char*);

char** search_record (char*);

struct Record;
Record* display_records (char*);

int get_record_count();